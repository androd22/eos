<form method="post" action="{{ path('app_admin_celebrity_delete', {'id': celebrity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ celebrity.id) }}">
    <button class="btn">Delete</button>
</form>
